# Copy this file to .env.local and adjust as needed

# === DEVELOPMENT MODE (Proxy ke Localhost) ===
# Kosongkan VITE_API_BASE_URL, isi VITE_PROXY_TARGET dengan backend lokal
VITE_API_BASE_URL=
VITE_PROXY_TARGET=http://localhost:8080

# === TELEGRAM TESTING (Proxy ke Cloudflare) ===
# Kosongkan VITE_API_BASE_URL, isi VITE_PROXY_TARGET dengan tunnel URL
# VITE_API_BASE_URL=
# VITE_PROXY_TARGET=https://parker-potato-consist-ranks.trycloudflare.com

# === PRODUCTION (Direct URL) ===
# Isi VITE_API_BASE_URL dengan full URL, kosongkan VITE_PROXY_TARGET
# VITE_API_BASE_URL=https://api.production.com/api
# VITE_PROXY_TARGET=
